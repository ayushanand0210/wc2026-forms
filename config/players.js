// Optional fixed roster. Leave empty ([]) to let players type their own name on
// the landing page (current default). If you fill in the 6 names here, the name
// box becomes a dropdown instead — this prevents typos and keeps names matching
// exactly across all three forms for scoring.
//
// Example: window.PLAYERS = ["Ayush", "Rohan", "Priya", "Sameer", "Neha", "Kabir"];
window.PLAYERS = [];
