// Paste your Google Apps Script Web App URL between the quotes below.
// (See README.md → "Wire up the Google Sheet" for the 5-minute setup.)
//
// Until this is filled in, submissions are saved to the browser's localStorage
// only (handy for testing the forms). Once you paste the URL, every submission
// is appended as a row to your Google Sheet.
//
// Example: window.SUBMIT_ENDPOINT = "https://script.google.com/macros/s/AKfy.../exec";
window.SUBMIT_ENDPOINT = "";
